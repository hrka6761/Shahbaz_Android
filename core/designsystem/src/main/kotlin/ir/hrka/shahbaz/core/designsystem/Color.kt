/** Defines the static light and dark color tokens used by the Shahbaz design system. */
package ir.hrka.shahbaz.core.designsystem

import androidx.compose.ui.graphics.Color

/** Light forest-green primary token used by the dark color scheme. */
val Forest80 = Color(0xFF82D5B4)

/** Light sky-blue secondary token used by the dark color scheme. */
val Sky80 = Color(0xFF9CCAFF)

/** Light amber tertiary token used by the dark color scheme. */
val Amber80 = Color(0xFFFFC977)

/** Dark forest-green primary token used by the light color scheme. */
val Forest40 = Color(0xFF006C4C)

/** Dark sky-blue secondary token used by the light color scheme. */
val Sky40 = Color(0xFF2E638D)

/** Dark amber tertiary token used by the light color scheme. */
val Amber40 = Color(0xFF855400)
